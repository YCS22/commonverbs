import { AiFillHeart } from "react-icons/ai";

const Score = ({ value }: { value: number }) => {
  return <div className='flex justify-center items-center'>Skor: {value}</div>;
};

export default Score;
